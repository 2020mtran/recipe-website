import React from 'react';

const HeroSection = ({ children }) => {
    return (
        <div class="flex flex-row w-full">
            <p>From <i>our</i> kitchen to <i>yours</i>, let's make Vietnamese food together!</p>
        </div>
    );
};

export default HeroSection;